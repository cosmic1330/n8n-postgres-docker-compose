# Allow local connections
local   all             all                                     trust
# Allow replication from any host
host    replication     replicator      0.0.0.0/0               scram-sha-256
# 允許所有使用者從任何 host 連線所有資料庫（測試/開發用）
host    all     all     0.0.0.0/0     scram-sha-256
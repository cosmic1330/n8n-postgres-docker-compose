# 1. 允許本地 Unix Socket 連線 (如 docker exec)
local   all             all                                     trust

# 2. 限制 root (Superuser) 僅能從容器內部 (127.0.0.1) 連線
host    all             root            127.0.0.1/32            trust
host    all             root            ::1/128                 trust
host    all             root            0.0.0.0/0               reject

# 3. 限制 n8n_user 只能從內網連線到 n8n 資料庫
host    n8n             n8n_user        172.25.0.0/16           scram-sha-256

# 4. 限制 app_writer 只能從內網連線到 app 資料庫 (Primary 僅允許寫入者)
host    app             app_writer      172.25.0.0/16           scram-sha-256
host    app             app_reader      0.0.0.0/0               reject

# 5. 允許資料庫同步 (Replication) - 強制使用 SSL
hostssl replication     replicator      172.25.0.0/16           scram-sha-256

# 6. 預設拒絕其他所有連線
host    all             all             0.0.0.0/0               reject